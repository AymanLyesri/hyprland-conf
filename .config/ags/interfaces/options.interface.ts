interface Settings
{
    waifu: {
        visibility: boolean,
        input_history: string,
    }
    rightPanel: {
        visibility: boolean,
        exclusivity: boolean,
        width: number,
        widgets: string[],
    }
}