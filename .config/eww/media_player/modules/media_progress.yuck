(defwidget media_progress[]
  
  (box
    :class  "media-progress"
    (scale
      :hexpand true
      :max 100
      :min 0
      :value {music_prog}
      :style "color:${music_title_color}"
      :onchange "bar/scripts/music.sh --set-pos {}"
    )
  )
  
  
)