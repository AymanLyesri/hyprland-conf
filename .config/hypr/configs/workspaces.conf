#apply the configuration to every workspace

# special workspace
# workspace = special:special, on-created-empty:(hyprctl dispatch exec "[special;float;size 20% 10%;move 40% 5%] kitty pipes.sh" & hyprctl dispatch exec "[special] kitty")
workspace = special:special, on-created-empty:kitty
